{
  "name": "edunest-saas",
  "private": true,
  "version": "0.2.1",
  "description": "Run backend + Next.js website + React panel together",
  "type": "module",
  "scripts": {
    "install:all": "npm install && npm --prefix website install && npm --prefix panel install",
    "setup": "node scripts/start-all.mjs --seed-only",
    "dev": "node scripts/start-all.mjs",
    "start": "node scripts/start-all.mjs",
    "dev:backend": "node scripts/start-all.mjs --seed-only && node -e \"console.log('Use npm start to run all services')\"",
    "dev:website": "npm --prefix website run dev -- -H 0.0.0.0 -p 3000",
    "dev:panel": "npm --prefix panel run dev -- --host 0.0.0.0 --port 5173",
    "seed": "node scripts/start-all.mjs --seed-only",
    "docker:up": "docker compose up --build",
    "docker:down": "docker compose down"
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}
